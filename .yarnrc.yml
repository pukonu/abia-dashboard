# Amplify (and any machine without a global yarn config) must use real
# node_modules — Yarn PnP has no node_modules tree, which breaks Turbopack's
# resolution of next/package.json and fails the build.
nodeLinker: node-modules
